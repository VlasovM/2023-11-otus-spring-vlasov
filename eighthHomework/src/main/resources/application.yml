spring:
  data:
    mongodb:
      port: 27017
      database: library

mongock:
  runner-type: "initializingbean"
  change-logs-scan-package: ru.javlasov.eighthomework.mongock.changelog
  mongo-db:
    write-concern:
      journal: false
    read-concern: local

